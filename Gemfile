source "https://rubygems.org"

[
  'railties',        # rails (Rails::Application, Engine, Railtie, Configuration)
  'activesupport',   # active_support (CoreExt, Autoload/Dependencies, Logging, Caching, Security, Notifications, i18n, Encoding)
  'actionpack',      # action_controller (http routing. includes action_view)
  'actionview',      # action_view (renderer/resolver)
  # 'activerecord',
  # 'actionmailer',
  # 'activejob',
  'bundler',
  # 'sprockets-rails'
].each do |app|
  gem app
end
